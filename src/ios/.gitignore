# SPDX-FileCopyrightText: Copyright 2026 eden-ios contributors
# SPDX-License-Identifier: GPL-3.0-or-later

# Generated by `xcodegen generate` from project.yml. project.yml is the source of
# truth; a committed pbxproj would drift from it silently and produce merge conflicts
# nobody can read.
Eden-iOS.xcodeproj/

# Written by ci/generate-link-flags.sh as an Xcode preBuildScript on every build.
# Contains absolute paths into the CMake build tree, so it is machine-specific and
# must never be committed.
link-flags.txt

# xcodebuild output when run by hand rather than through the workflow.
build/
DerivedData/

# Staged by ci/fetch-moltenvk.sh.
moltenvk/

.DS_Store
